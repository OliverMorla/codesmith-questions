const num = 40;
let final;
// ADD CODE HERE
if (num > 100) {
  // if num is greater than 100, assign 'final' to null
  final = null;
}

// if num is less than 100, multiply num * 2
final = num * 2;

// Log final to the console below to check your work
console.log(final);
